services:
  kore-creative-os:
    build: .
    container_name: kore-creative-os
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      REPLICATE_API_TOKEN: ${REPLICATE_API_TOKEN}
    volumes:
      - creative_data:/app/.wrangler

volumes:
  creative_data:
